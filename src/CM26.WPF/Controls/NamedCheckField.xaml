<UserControl x:Class="CM26.Studio.Controls.NamedCheckField"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="0,2">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="140" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock x:Name="LabelText" Grid.Column="0" Margin="0,0,8,0" TextTrimming="CharacterEllipsis" />

        <CheckBox x:Name="Check" Grid.Column="1" Checked="Check_Changed" Unchecked="Check_Changed" />
    </Grid>
</UserControl>
