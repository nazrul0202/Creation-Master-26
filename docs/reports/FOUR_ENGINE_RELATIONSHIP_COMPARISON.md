# Four-engine relationship comparison

Current CM26 has existing country-confederation and relationship diagnostics, but the requested country matrix and same-file four-engine raw comparisons were not rerun in this audit. Status: **NOT TESTED** rather than assumed.

The local reference cannot validate current relationship values because its main DB and XML hashes differ. No hard-coded mapping and no resolver change was introduced.
